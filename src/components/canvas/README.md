# Canvas Components

This directory is for Canvas-related components.

## Rules:

- Use strict naming conventions, e.g., `EditorMainCanvas.tsx`.
- No inline functions in event handlers (use `useCallback`).
- No `any` type usage.
